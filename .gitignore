# .NET build output
bin/
obj/
*.user
*.suo
.vs/
*.rsuser

# EF Core migrations snapshot backup (keep Migrations/ folder itself)
# Uncomment if you want to exclude snapshots: *ModelSnapshot.cs

# ASP.NET Core secrets — NEVER commit real keys
appsettings.Development.json
appsettings.Production.json
appsettings.*.local.json
secrets.json

# Node / React
node_modules/
dist/
.env
.env.local
.env.*.local

# Vite cache
TaskFlow.Web/.vite/

# OS
.DS_Store
Thumbs.db

# Azure publish profiles (contain credentials)
*.pubxml
*.pubxml.user
